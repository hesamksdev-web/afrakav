# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log

# OS
.DS_Store

# TLS material — never commit certificates or keys
certs/

# The Dana font package as delivered by the foundry: PDFs, the 13 static cuts
# and format variants. Only the variable webfont the app actually loads is
# committed, under src/assets/fonts/ with its licence beside it.
DanaPro(13Wt)/

# Source artwork dropped in the repo root; the app loads src/assets/afranet-logo.webp
new-logo.webp
